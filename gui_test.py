# ATTENTION !!
# L'INTERFACE EST EXPERIMENTALE, IL SE PEUX QU'ON NE PUISSE PAS LA FINIR...
# (LE CODE CI-DESSOUS EST POURRI, MAIS C PAS GRAVE :P)

def print_scoreboard(root, players, fields=None, limit=-1):

    # si fields n'est pas donné, alors le déduire du premier perso.
    if fields is None:
        fields = [i for i in players[0]]



def isTrue(b):
    """
    Cette fonction prends détermine si b est vrai (True si vrai, False si faux).
    """
    if b == True:
        return True
    else:
        return False
        